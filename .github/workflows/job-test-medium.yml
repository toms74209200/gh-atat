name: Job - Test (medium size)

on:
  workflow_call:

jobs:
  test:
    name: Run Medium Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25.5'
          cache: true

      - name: Tidy dependencies
        run: go mod tidy

      - name: Run medium tests
        run: go test -tags=medium ./...